<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="description" content="A clean, modern template website you can customize for any service business." />
  <title>DriveTemplate — Service Site Template</title>

  <style>
    :root{
      --bg:#0b1020;
      --panel: rgba(255,255,255,.08);
      --panel2: rgba(255,255,255,.12);
      --border: rgba(255,255,255,.14);
      --text:#eaf0ff;
      --muted: rgba(234,240,255,.75);
      --accent:#6ea8ff;
      --accent2:#7cf0d6;
      --shadow: 0 18px 60px rgba(0,0,0,.45);
      --radius: 18px;
      --max: 1100px;
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      color:var(--text);
      background:
        radial-gradient(1200px 700px at 20% 0%, rgba(110,168,255,.22), transparent 60%),
        radial-gradient(900px 600px at 90% 20%, rgba(124,240,214,.16), transparent 60%),
        radial-gradient(900px 700px at 60% 100%, rgba(110,168,255,.12), transparent 60%),
        linear-gradient(180deg, #070a14 0%, #0b1020 45%, #070a14 100%);
      overflow-x:hidden;
    }

    a{color:inherit; text-decoration:none}
    .container{width:min(var(--max), calc(100% - 40px)); margin:0 auto;}

    /* Banner */
    .banner{
      position:relative;
      min-height: 82vh;
      display:flex;
      align-items:stretch;
      overflow:hidden;
      border-bottom: 1px solid rgba(255,255,255,.06);
    }
    .banner::before{
      content:"";
      position:absolute; inset:0;
      background:
        linear-gradient(180deg, rgba(7,10,20,.2) 0%, rgba(7,10,20,.9) 75%, rgba(7,10,20,1) 100%),
        url("https://images.unsplash.com/photo-1523966211575-eb4a01e7dd51?auto=format&fit=crop&w=2400&q=80");
      background-size:cover;
      background-position:center;
      filter:saturate(1.05) contrast(1.05);
      transform:scale(1.04);
    }
    .banner::after{
      content:"";
      position:absolute; inset:-2px;
      background: radial-gradient(900px 500px at 15% 35%, rgba(110,168,255,.28), transparent 60%);
      pointer-events:none;
    }

    /* Glass top nav */
    .topbar{
      position:sticky;
      top:0;
      z-index:50;
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      background: rgba(10,14,28,.48);
      border-bottom: 1px solid rgba(255,255,255,.08);
    }
    .topbar-inner{
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding: 12px 0;
    }
    .brand{
      display:flex;
      align-items:center;
      gap:12px;
      min-width: 220px;
    }
    .logo{
      width:42px; height:42px;
      border-radius: 14px;
      box-shadow: 0 10px 30px rgba(0,0,0,.35);
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.12);
      display:grid; place-items:center;
      overflow:hidden;
    }
    .brand .name{
      display:flex; flex-direction:column; line-height:1.05;
    }
    .brand .name strong{font-size: 14.5px; letter-spacing:.2px}
    .brand .name span{font-size: 12px; color:var(--muted)}

    .nav-actions{
      display:flex; align-items:center; gap:10px;
    }
    .pill{
      display:inline-flex; align-items:center; gap:10px;
      padding: 10px 14px;
      border-radius: 999px;
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.12);
      color: var(--text);
      box-shadow: 0 10px 40px rgba(0,0,0,.25);
      cursor:pointer;
      user-select:none;
      transition: transform .15s ease, background .15s ease, border-color .15s ease;
    }
    .pill:hover{ transform: translateY(-1px); background: rgba(255,255,255,.085); border-color: rgba(255,255,255,.18); }
    .pill:active{ transform: translateY(0px) scale(.99); }

    .hamburger{
      width:18px; height:12px; position:relative;
    }
    .hamburger span{
      position:absolute; left:0; right:0; height:2px;
      background: rgba(234,240,255,.9);
      border-radius: 2px;
    }
    .hamburger span:nth-child(1){top:0}
    .hamburger span:nth-child(2){top:5px; opacity:.85}
    .hamburger span:nth-child(3){bottom:0; opacity:.7}

    /* Sidebar */
    .overlay{
      position:fixed; inset:0;
      background: rgba(0,0,0,.55);
      opacity:0; pointer-events:none;
      transition: opacity .2s ease;
      z-index:80;
    }
    .overlay.open{opacity:1; pointer-events:auto;}
    .sidebar{
      position:fixed;
      top:0; right:0;
      width:min(360px, 88vw);
      height:100%;
      transform: translateX(102%);
      transition: transform .25s ease;
      z-index:90;
      background: rgba(10,14,28,.75);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border-left: 1px solid rgba(255,255,255,.12);
      box-shadow: -30px 0 70px rgba(0,0,0,.45);
      padding: 18px;
      display:flex;
      flex-direction:column;
      gap:12px;
    }
    .sidebar.open{transform: translateX(0);}
    .sidebar-header{
      display:flex; align-items:center; justify-content:space-between;
      gap:10px;
      padding-bottom:10px;
      border-bottom: 1px solid rgba(255,255,255,.10);
    }
    .xbtn{
      width:42px; height:42px; border-radius: 14px;
      display:grid; place-items:center;
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.12);
      cursor:pointer;
    }
    .side-links{
      display:flex;
      flex-direction:column;
      gap:10px;
      padding-top: 6px;
    }
    .side-links a{
      padding: 12px 12px;
      border-radius: 14px;
      background: rgba(255,255,255,.05);
      border: 1px solid rgba(255,255,255,.10);
      color: rgba(234,240,255,.92);
      transition: background .15s ease, transform .15s ease, border-color .15s ease;
    }
    .side-links a:hover{
      background: rgba(255,255,255,.075);
      border-color: rgba(255,255,255,.16);
      transform: translateY(-1px);
    }
    .side-note{
      margin-top:auto;
      color: var(--muted);
      font-size: 12px;
      line-height:1.35;
      border-top: 1px solid rgba(255,255,255,.10);
      padding-top:12px;
    }

    /* Hero content */
    .hero{
      position:relative;
      z-index:2;
      width:100%;
      display:flex;
      align-items:center;
    }
    .hero-grid{
      padding: 64px 0 52px;
      display:grid;
      grid-template-columns: 1.15fr .85fr;
      gap: 26px;
      align-items:center;
    }
    @media (max-width: 920px){
      .hero-grid{ grid-template-columns: 1fr; padding: 46px 0 42px; }
    }

    .card{
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
    }
    .hero-card{
      padding: 26px;
    }
    .kicker{
      display:inline-flex; align-items:center; gap:8px;
      font-size: 12px;
      letter-spacing: .35px;
      text-transform: uppercase;
      color: rgba(234,240,255,.82);
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.12);
      border-radius: 999px;
      padding: 7px 10px;
    }
    .dot{
      width:8px;height:8px;border-radius:50%;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      box-shadow: 0 0 0 4px rgba(110,168,255,.14);
    }
    h1{
      margin: 14px 0 10px;
      font-size: clamp(34px, 4.6vw, 54px);
      letter-spacing:-.6px;
      line-height:1.03;
    }
    .lead{
      color: var(--muted);
      font-size: 15.5px;
      line-height: 1.55;
      margin: 0 0 18px;
      max-width: 62ch;
    }
    .cta-row{
      display:flex;
      flex-wrap:wrap;
      gap: 10px;
      margin-top: 4px;
    }
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      gap: 10px;
      padding: 12px 15px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,.12);
      background: rgba(255,255,255,.06);
      color: var(--text);
      cursor:pointer;
      transition: transform .15s ease, background .15s ease, border-color .15s ease;
      font-weight: 600;
      font-size: 13.5px;
    }
    .btn:hover{ transform: translateY(-1px); background: rgba(255,255,255,.085); border-color: rgba(255,255,255,.18); }
    .btn.primary{
      background: linear-gradient(135deg, rgba(110,168,255,.22), rgba(124,240,214,.16));
      border-color: rgba(124,240,214,.22);
    }
    .tiny{
      font-size: 12px; color: var(--muted);
    }

    .stats{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 10px;
      padding: 16px;
    }
    .stat{
      padding: 14px;
      border-radius: 16px;
      background: rgba(255,255,255,.05);
      border: 1px solid rgba(255,255,255,.10);
    }
    .stat strong{display:block; font-size: 18px;}
    .stat span{display:block; margin-top:4px; font-size:12px; color: var(--muted);}

    /* Sections */
    section{ padding: 54px 0; }
    .section-head{
      display:flex;
      align-items:flex-end;
      justify-content:space-between;
      gap: 16px;
      margin-bottom: 16px;
    }
    h2{ margin:0; font-size: 24px; letter-spacing:-.3px;}
    .sub{ margin:0; color: var(--muted); font-size: 13.5px; line-height:1.5; max-width: 68ch;}

    .grid-3{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 14px;
    }
    @media (max-width: 900px){ .grid-3{grid-template-columns: 1fr;} }

    .feature{
      padding: 16px;
    }
    .feature .icon{
      width:40px;height:40px;border-radius: 14px;
      display:grid; place-items:center;
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.12);
      margin-bottom: 10px;
    }
    .feature h3{ margin: 0 0 6px; font-size: 16px;}
    .feature p{ margin:0; color: var(--muted); font-size: 13.5px; line-height:1.55; }

    /* Contact */
    .contact{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
      align-items:stretch;
    }
    @media (max-width: 900px){ .contact{grid-template-columns: 1fr;} }

    form{ padding: 18px; }
    label{ display:block; font-size: 12px; color: var(--muted); margin: 10px 0 6px; }
    input, textarea{
      width:100%;
      padding: 12px 12px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,.12);
      background: rgba(0,0,0,.18);
      color: var(--text);
      outline:none;
    }
    textarea{ min-height: 110px; resize: vertical; }
    input::placeholder, textarea::placeholder{ color: rgba(234,240,255,.45); }

    .toast{
      position:fixed;
      left:50%;
      bottom: 18px;
      transform: translateX(-50%) translateY(20px);
      opacity:0;
      pointer-events:none;
      transition: opacity .2s ease, transform .2s ease;
      z-index:120;
      background: rgba(10,14,28,.82);
      border: 1px solid rgba(255,255,255,.14);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      padding: 10px 12px;
      border-radius: 14px;
      box-shadow: var(--shadow);
      color: rgba(234,240,255,.92);
      font-size: 13px;
    }
    .toast.show{ opacity:1; transform: translateX(-50%) translateY(0); }

    footer{
      padding: 26px 0 40px;
      border-top: 1px solid rgba(255,255,255,.08);
      color: var(--muted);
      font-size: 12.5px;
    }
    footer .row{
      display:flex;
      flex-wrap:wrap;
      gap: 10px 14px;
      align-items:center;
      justify-content:space-between;
    }
    .footer-links{
      display:flex; gap: 12px; flex-wrap:wrap;
    }
    .footer-links a{
      color: rgba(234,240,255,.75);
    }
    .footer-links a:hover{ color: rgba(234,240,255,.95); }
  </style>
</head>

<body>
  <!-- Topbar -->
  <div class="topbar">
    <div class="container">
      <div class="topbar-inner">
        <a class="brand" href="#top" aria-label="Home">
          <div class="logo" aria-hidden="true">
            <!-- New generic logo (inline SVG) -->
            <svg width="30" height="30" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
              <defs>
                <linearGradient id="g" x1="10" y1="6" x2="58" y2="56" gradientUnits="userSpaceOnUse">
                  <stop stop-color="#6EA8FF"/>
                  <stop offset="1" stop-color="#7CF0D6"/>
                </linearGradient>
              </defs>
              <path d="M12 38c0-14 10-26 26-26 8 0 14 2 20 7l-7 7c-4-3-8-4-13-4-10 0-16 7-16 16 0 9 6 16 16 16 6 0 10-2 14-5l7 7c-6 5-13 8-22 8C22 64 12 52 12 38Z" fill="url(#g)"/>
              <path d="M48 18l4 4-24 24-12-12 4-4 8 8 20-20Z" fill="rgba(234,240,255,.92)"/>
            </svg>
          </div>
          <div class="name">
            <strong>DriveTemplate</strong>
            <span>Service Website Template</span>
          </div>
        </a>

        <div class="nav-actions">
          <a class="pill" href="#contact" title="Jump to contact">
            <span style="opacity:.9;font-weight:700;">Contact</span>
          </a>

          <button class="pill" id="menuBtn" type="button" aria-label="Open menu">
            <div class="hamburger" aria-hidden="true">
              <span></span><span></span><span></span>
            </div>
            <span style="opacity:.9;font-weight:700;">Menu</span>
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Sidebar + overlay -->
  <div class="overlay" id="overlay" aria-hidden="true"></div>
  <aside class="sidebar" id="sidebar" aria-label="Sidebar navigation">
    <div class="sidebar-header">
      <div style="display:flex;align-items:center;gap:10px;">
        <div class="logo" style="width:40px;height:40px;border-radius:14px;">
          <svg width="26" height="26" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <defs>
              <linearGradient id="g2" x1="10" y1="6" x2="58" y2="56" gradientUnits="userSpaceOnUse">
                <stop stop-color="#6EA8FF"/>
                <stop offset="1" stop-color="#7CF0D6"/>
              </linearGradient>
            </defs>
            <path d="M12 38c0-14 10-26 26-26 8 0 14 2 20 7l-7 7c-4-3-8-4-13-4-10 0-16 7-16 16 0 9 6 16 16 16 6 0 10-2 14-5l7 7c-6 5-13 8-22 8C22 64 12 52 12 38Z" fill="url(#g2)"/>
            <path d="M48 18l4 4-24 24-12-12 4-4 8 8 20-20Z" fill="rgba(234,240,255,.92)"/>
          </svg>
        </div>
        <div>
          <div style="font-weight:800; letter-spacing:.2px;">DriveTemplate</div>
          <div style="font-size:12px;color:var(--muted); margin-top:2px;">Replace links + sections</div>
        </div>
      </div>

      <button class="xbtn" id="closeBtn" type="button" aria-label="Close menu">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <path d="M6 6l12 12M18 6L6 18" stroke="rgba(234,240,255,.9)" stroke-width="2.2" stroke-linecap="round"/>
        </svg>
      </button>
    </div>

    <nav class="side-links">
      <a href="#top" data-close>Home</a>
      <a href="#services" data-close>Services</a>
      <a href="#how" data-close>How It Works</a>
      <a href="#about" data-close>About</a>
      <a href="#contact" data-close>Contact</a>
    </nav>

    <div class="side-note">
      <strong style="color:rgba(234,240,255,.9);">Template note:</strong>
      Replace the banner image, business name, and text. This file is designed to be copied for new clients/projects.
    </div>
  </aside>

  <!-- Banner / Hero -->
  <header class="banner" id="top">
    <div class="container hero">
      <div class="hero-grid">
        <div class="card hero-card">
          <div class="kicker"><span class="dot"></span> Modern one-page template</div>
          <h1>Your Service Business,</h1>
          <h1 style="margin-top:-6px;">Presented Clean & Professional</h1>
          <p class="lead">
            This is a template version of your AllDrive-style site—ready for any business.
            Swap the name, logo, hero text, services, and contact details, and you’ve got a new client-ready website.
          </p>

          <div class="cta-row">
            <a class="btn primary" href="#services">View Services</a>
            <a class="btn" href="#contact">Request a Quote</a>
          </div>

          <p class="tiny" style="margin-top:12px;">
            Tip: You can keep this as a base and duplicate the repo for each new brand/project.
          </p>
        </div>

        <div class="card stats" aria-label="Quick stats">
          <div class="stat">
            <strong>Fast</strong>
            <span>Single-file template</span>
          </div>
          <div class="stat">
            <strong>Clean</strong>
            <span>Glass + dark theme</span>
          </div>
          <div class="stat">
            <strong>Flexible</strong>
            <span>Any service niche</span>
          </div>
          <div class="stat">
            <strong>Mobile</strong>
            <span>Responsive layout</span>
          </div>
          <div class="stat">
            <strong>No build</strong>
            <span>Just HTML/CSS/JS</span>
          </div>
          <div class="stat">
            <strong>GitHub Pages</strong>
            <span>Free hosting</span>
          </div>
        </div>
      </div>
    </div>
  </header>

  <!-- Services -->
  <section id="services">
    <div class="container">
      <div class="section-head">
        <div>
          <h2>Services</h2>
          <p class="sub">Replace these cards with your real offerings. Keep them short and benefit-focused.</p>
        </div>
      </div>

      <div class="grid-3">
        <div class="card feature">
          <div class="icon" aria-hidden="true">⚙️</div>
          <h3>Service Type #1</h3>
          <p>Explain what you do, who it’s for, and what problem it solves—without getting too technical.</p>
        </div>

        <div class="card feature">
          <div class="icon" aria-hidden="true">🛠️</div>
          <h3>Service Type #2</h3>
          <p>Highlight what makes you different: speed, convenience, quality, transparency, or specialty.</p>
        </div>

        <div class="card feature">
          <div class="icon" aria-hidden="true">✅</div>
          <h3>Service Type #3</h3>
          <p>Add your “most requested” option here. This is where many visitors decide to contact you.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- How it works -->
  <section id="how">
    <div class="container">
      <div class="section-head">
        <div>
          <h2>How It Works</h2>
          <p class="sub">A simple 3-step process builds trust and makes it easy for people to take action.</p>
        </div>
      </div>

      <div class="grid-3">
        <div class="card feature">
          <div class="icon" aria-hidden="true">1</div>
          <h3>Step One</h3>
          <p>What the customer does first (example: send a message, request a quote, pick a time).</p>
        </div>
        <div class="card feature">
          <div class="icon" aria-hidden="true">2</div>
          <h3>Step Two</h3>
          <p>What you do next (example: confirm details, provide estimate, schedule service).</p>
        </div>
        <div class="card feature">
          <div class="icon" aria-hidden="true">3</div>
          <h3>Step Three</h3>
          <p>Deliver the result (example: complete service, follow-up, warranty/guarantee).</p>
        </div>
      </div>
    </div>
  </section>

  <!-- About -->
  <section id="about">
    <div class="container">
      <div class="section-head">
        <div>
          <h2>About</h2>
          <p class="sub">
            Keep this short. Use 2–4 sentences that establish credibility, location, and what you value.
          </p>
        </div>
      </div>

      <div class="card" style="padding:18px;">
        <p style="margin:0;color:var(--muted);line-height:1.65;">
          <strong style="c
